{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,re; d=json.load(sys.stdin); c=d.get('command',''); blocked=bool(re.search(r'git\\s+push\\b.*\\bmain\\b',c)); print('Direct push to main is not allowed. Use a feature branch and open a PR.') if blocked else None; sys.exit(2 if blocked else 0)\""
          }
        ]
      }
    ]
  }
}
