version: '3.8'

services:
  tests-shard-1:
    build: 
      context: ..
      dockerfile: docker/Dockerfile
    # Updated to use the universal project 'chromium'
    command: npx playwright test --shard=1/2 --project=chromium
    environment:
      - BASE_URL=${BASE_URL}
      - API_URL=${API_URL}
      - ADMIN_USERNAME=${ADMIN_USERNAME}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
    volumes:
      - ../results/shard-1:/app/test-results

  tests-shard-2:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    # Updated to use the universal project 'chromium'
    command: npx playwright test --shard=2/2 --project=chromium
    environment:
      - BASE_URL=${BASE_URL}
      - API_URL=${API_URL}
      - ADMIN_USERNAME=${ADMIN_USERNAME}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
    volumes:
      - ../results/shard-2:/app/test-results
